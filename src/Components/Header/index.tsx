import { DataRange } from '../DataRange'
import { HeaderContainer } from './Header.styles'

export const Header = () => {
  return (
    <HeaderContainer>
      <DataRange />
    </HeaderContainer>
  )
}
